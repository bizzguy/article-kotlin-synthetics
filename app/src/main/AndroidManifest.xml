<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.article.kotlinsynthetics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".a_java_findviewbyid.MainActivity"
            android:exported="true" />
        <activity
            android:name=".b_java_butterknife.MainActivity"
            android:exported="true" />
        <activity
            android:name=".c_kotlin_butterknife.MainActivity"
            android:exported="true" />
        <activity
            android:name=".d_kotlin_synthetics.MainActivity"
            android:exported="true" />
        <activity
            android:name=".e_kotlin_synthetics_no_fragment.MainActivity"
            android:exported="true" />

    </application>

</manifest>